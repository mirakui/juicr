/*
var TWITTER_BASE_URI = 'http://twitter.com';

var Juicr = {
  test: function(query) {
    $.getJSON(
      TWITTER_BASE_URI + '/search.json?q=' + query + '&callback=?',
      function(data) { 
        
      });
  }
  createCell: function() {

              }
}
*/
